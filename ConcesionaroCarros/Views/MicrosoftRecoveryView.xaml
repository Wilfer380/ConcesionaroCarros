<Window x:Class="ConcesionaroCarros.Views.MicrosoftRecoveryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Validacion Microsoft"
        Width="700"
        Height="720"
        MinWidth="700"
        MinHeight="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource CorpWindowBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="MsTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource CorpBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="MsPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource CorpBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="{StaticResource CorpAccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource CorpAccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource CorpTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CorpBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Border Padding="32"
                CornerRadius="18"
                Background="{StaticResource CorpSurfaceBrush}"
                BorderBrush="{StaticResource CorpBorderBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,18">
                    <Grid Width="42" Height="42" HorizontalAlignment="Left" Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Rectangle Fill="#F25022" Margin="0,0,2,2"/>
                        <Rectangle Grid.Column="1" Fill="#7FBA00" Margin="2,0,0,2"/>
                        <Rectangle Grid.Row="1" Fill="#00A4EF" Margin="0,2,2,0"/>
                        <Rectangle Grid.Row="1" Grid.Column="1" Fill="#FFB900" Margin="2,2,0,0"/>
                    </Grid>

                    <TextBlock Text="Microsoft"
                               FontSize="34"
                               Foreground="{StaticResource CorpAccentBrush}"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Validacion de identidad"
                               FontSize="24"
                               Margin="0,12,0,6"
                               Foreground="{StaticResource CorpTextBrush}"/>
                    <TextBlock Text="Recuperacion local con verificacion visual y cambio directo de contrasena."
                               Foreground="{StaticResource CorpMutedBrush}"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <Border Grid.Row="1"
                        Background="#F2F7FB"
                        BorderBrush="{StaticResource CorpBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="14"
                        Margin="0,0,0,18"
                        Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding MensajeEstado}"
                               Foreground="{StaticResource CorpTextBrush}"
                               TextWrapping="Wrap"/>
                </Border>

                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="0,0,4,0">
                    <Grid>
                        <StackPanel Visibility="{Binding IsValidationStep, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Correo registrado"
                                       FontSize="14"
                                       Foreground="#374151"
                                       Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource MsTextBox}"
                                     Text="{Binding Correo, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="correo registrado en el sistema"/>
                            <CheckBox Content="No soy un robot"
                                      IsChecked="{Binding NoSoyRobot}"
                                      Margin="0,20,0,0"
                                      FontSize="15"
                                      Foreground="#111827"/>
                            <Border Margin="0,18,0,0"
                                    Padding="16"
                                    Background="#F9FBFF"
                                    BorderBrush="#DCE6F5"
                                    BorderThickness="1"
                                    CornerRadius="12">
                                <TextBlock Text="Al validar, el sistema mostrara un codigo temporal en una ventana emergente durante 5 segundos y luego te permitira definir una nueva contrasena."
                                           Foreground="#4B5563"
                                           TextWrapping="Wrap"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsPasswordStep, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Correo validado"
                                       FontSize="14"
                                       Foreground="#374151"
                                       Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource MsTextBox}"
                                 Text="{Binding Correo}"
                                 IsReadOnly="True"
                                 Background="#F9FAFB"/>
                        <TextBlock Text="Codigo de validacion"
                                   FontSize="14"
                                   Foreground="#374151"
                                   Margin="0,18,0,6"/>
                        <TextBox Style="{StaticResource MsTextBox}"
                                 Text="{Binding CodigoVerificacion, UpdateSourceTrigger=PropertyChanged}"
                                 MaxLength="6"/>
                        <TextBlock Text="Nueva contrasena"
                                   FontSize="14"
                                   Foreground="#374151"
                                   Margin="0,18,0,6"/>
                            <PasswordBox x:Name="txtNuevaPassword"
                                         Style="{StaticResource MsPasswordBox}"
                                         PasswordChanged="NuevaPasswordChanged"/>
                            <TextBlock Text="Confirmar contrasena"
                                       FontSize="14"
                                       Foreground="#374151"
                                       Margin="0,18,0,6"/>
                            <PasswordBox x:Name="txtConfirmarPassword"
                                         Style="{StaticResource MsPasswordBox}"
                                         PasswordChanged="ConfirmarPasswordChanged"/>
                        <TextBlock Text="La nueva contrasena se guardara directamente en la base de datos cuando el codigo sea correcto, y el login principal quedara precargado con las nuevas credenciales."
                                   Foreground="#6B7280"
                                   Margin="0,12,0,0"
                                   TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

                <Border Grid.Row="3"
                        BorderBrush="#E5EAF2"
                        BorderThickness="0,1,0,0"
                        Margin="0,22,0,0"
                        Padding="0,18,0,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Visibility="{Binding IsValidationStep, Converter={StaticResource BoolToVis}}">
                            <Button Content="Cancelar"
                                    Width="118"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,10,0"
                                    Command="{Binding CancelarCommand}"/>
                            <Button Content="Validar"
                                    Width="128"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding ValidarIdentidadCommand}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Visibility="{Binding IsPasswordStep, Converter={StaticResource BoolToVis}}">
                            <Button Content="Cancelar"
                                    Width="118"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,10,0"
                                    Command="{Binding CancelarCommand}"/>
                            <Button Content="Volver"
                                    Width="118"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,10,0"
                                    Command="{Binding VolverCommand}"/>
                            <Button Content="Cambiar contrasena"
                                    Width="170"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding CambiarPasswordCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
