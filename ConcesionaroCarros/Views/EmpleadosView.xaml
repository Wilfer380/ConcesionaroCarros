<UserControl x:Class="ConcesionaroCarros.Views.EmpleadosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1080">

    <UserControl.Resources>
        <Style x:Key="EmployeeCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="240"/>
        </Style>

        <Style x:Key="GoldActionButton" TargetType="Button">
            <Setter Property="Background" Value="#D4AF37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

     
        <DockPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="EQUIPO DE VENTAS" FontSize="32" FontWeight="ExtraBold"/>
                <TextBlock Text="Panel de administración de talento" FontSize="16" Foreground="#666"/>
            </StackPanel>

            <Button DockPanel.Dock="Right"
                    Content="+ Nuevo Empleado"
                    Width="180"
                    Height="45"
                    Style="{StaticResource GoldActionButton}"
                    Command="{Binding NuevoEmpleadoCommand}"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Empleados}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource EmployeeCardStyle}">
                            <Grid Margin="15">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel>
                                    <TextBlock Text="{Binding Nombres}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Cargo}" Foreground="#D4AF37"/>
                                </StackPanel>


                                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                                    <TextBlock Text="{Binding Correo}"/>
                                    <TextBlock Text="{Binding Telefono}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="✎"
                                            Width="30"
                                            Height="30"
                                            Margin="0,0,5,0"
                                            Style="{StaticResource GoldActionButton}"
                                            Command="{Binding DataContext.EditarEmpleadoCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <ContentControl Content="{Binding ModalView}" Panel.ZIndex="99"/>
    </Grid>
</UserControl>
