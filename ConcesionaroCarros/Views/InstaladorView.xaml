<UserControl x:Class="ConcesionaroCarros.Views.InstaladorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="40">

        <StackPanel HorizontalAlignment="Center" Width="400">

            <TextBlock Text="Seleccionar instalador"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <Border Height="120"
                    Margin="0,20"
                    BorderBrush="#CCC"
                    BorderThickness="1"
                    CornerRadius="6">

                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                    <Image Width="48"
                           Height="48"
                           Source="{Binding Icono}"/>

                    <TextBlock Text="{Binding NombreArchivo}"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"/>

                </StackPanel>
            </Border>

            <Button Content="Buscar instalador"
                    Command="{Binding BuscarExeCommand}"
                    Height="40"
                    Margin="0,10"/>

            <Button Content="Ejecutar instalador"
                    Command="{Binding EjecutarCommand}"
                    Height="40"
                    Background="#111"
                    Foreground="White"/>

        </StackPanel>

    </Grid>
</UserControl>
