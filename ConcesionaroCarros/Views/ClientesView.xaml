<UserControl x:Class="ConcesionaroCarros.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="https://images.unsplash.com/photo-1492144534655-ae79c964c9d7?q=80&amp;w=1983&amp;auto=format&amp;fit=crop" 
                        Stretch="UniformToFill" Opacity="0.3"/>
        </Grid.Background>

        <Border Background="#F2FFFFFF" 
                CornerRadius="15" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center" 
                Padding="40" 
                Width="500" 
                BorderBrush="#DDD" 
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="REGISTRO DE CLIENTE" 
                           FontSize="22" 
                           FontWeight="ExtraBold" 
                           Foreground="#1E1E1E" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,30"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.Resources>
                        <Style TargetType="TextBlock" x:Key="LabelStyle">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#555"/>
                            <Setter Property="Margin" Value="0,0,15,20"/>
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Margin" Value="0,0,0,20"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderBrush" Value="#CCC"/>
                        </Style>
                    </Grid.Resources>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Tag="Ej: Juan Pérez"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Cédula:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Tag="Documento de identidad"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Tag="correo@ejemplo.com"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Teléfono:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Tag="+57 300..."/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Dirección:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Tag="Calle, Carrera, Ciudad"/>
                </Grid>

                <Button Content="GUARDAR CLIENTE" 
                        Height="45" 
                        Margin="90,10,100,09" 
                        Background="#1E1E1E" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>